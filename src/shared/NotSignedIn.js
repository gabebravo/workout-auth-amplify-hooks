import React from 'react';

const NotFound = () =>
  <div style={{marginTop: '10%', textAlign: 'center' }}>
    <h3>Could Not Authenticate</h3>
    <p>We are sorry but the page you are looking for requires authenticaiton.</p>
  </div>

export default NotFound;